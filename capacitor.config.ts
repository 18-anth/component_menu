import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'componente_Menu',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
